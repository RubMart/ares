# Copiar a .env y ajustar. No versionar .env.

# App
DEBUG=false
CORS_ORIGINS=["*"]

# PostgreSQL (asyncpg)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/detecciones
CATALOG_TABLE=detecciones_catalogo

# Ollama (LLM local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:3b
OLLAMA_TEMPERATURE=0.0

# CLIP
CLIP_MODEL_NAME=clip-ViT-B-32
# Si falta, se descarga openai/clip-vit-base-patch32 aquí (relativo a api/)
CLIP_LOCAL_DIR=../models/clip-vit-base-patch32
CLIP_ONNX_BACKEND=false
EMBEDDING_DIM=512

# Búsqueda
DEFAULT_TOP_K=50
DEFAULT_PER_LAYER_LIMIT=100
DEFAULT_MIN_CONFIDENCE=0.0
DEFAULT_SPATIAL_DISTANCE_M=50.0
MAX_SPATIAL_DISTANCE_M=500.0
